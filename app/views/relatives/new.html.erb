<%= simple_form_for [@relative] do |f| %>
  <div class="form-inputs">
    <%= f.association :user, collection: User.all %>
  </div>
  
  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>

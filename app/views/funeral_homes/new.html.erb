<h1> funeral homes new</h1>

<%= simple_form_for [@funeral, @funeral_home] do |f| %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :address %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>

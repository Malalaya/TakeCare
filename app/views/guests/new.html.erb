<h1> Funeral guest new</h1>

<%= simple_form_for [@funeral, @guest] do |f| %>
  <div class="form-inputs">
    <%= f.input :guest_list %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'button-blue'  %>
  </div>
<% end %>

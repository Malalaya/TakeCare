<div class = "container">
<h1> Flowers</h1>

<%= simple_form_for [@funeral, @flower] do |f| %>
  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'button-blue' %>
  </div>
<% end %>
</div>
